services:
  redis:
    command: redis-server --bind 127.0.0.1 --port "$PORT" --save "" --appendonly no
    port: 6379
  web:
    command: npm start
    health: /
    portal:
      title: Code pastebin
      description: Local development instance using file storage by default.
